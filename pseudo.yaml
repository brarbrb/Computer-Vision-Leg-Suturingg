path: ./data/pseudo
train: images/train
val: images/val

names:
  0: Empty
  1: Tweezers
  2: Needle_driver
# This YAML file is used to configure a dataset for a machine learning model.
# It specifies the path to the dataset, the directories for training and validation images,
# the number of classes (nc), and the names of the classes.